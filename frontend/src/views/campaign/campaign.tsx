import Typography from "@mui/material/Typography";
import { Page } from "components";

const Campaign = () => {
  return (
    <Page>
      <Typography>Campaign Page</Typography>
    </Page>
  );
};

export default Campaign;
